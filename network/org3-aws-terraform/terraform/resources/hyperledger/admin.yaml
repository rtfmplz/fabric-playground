version: "2"

networks:
  default:
    external:
      name: ${DOCKER_NETWORK}

services:

  cli:
    extends:
      file: cli.yaml
      service: cli
    container_name: cli
    networks:
      - default
